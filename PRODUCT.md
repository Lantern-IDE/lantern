# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

(Tauri v2 데스크톱 앱. 화면은 WebView2 위의 HTML/CSS/TypeScript. 창 크기 최소 900×560, 주 사용 환경은 Windows 데스크톱.)

## Users

여러 AI 모델을 비교하며 자유롭게 쓰고 싶고, 도구를 직접 튜닝하고 뜯어고치는 걸 좋아하는 개인 개발자. 사이드 프로젝트부터 중간 규모 코드베이스까지 다룬다. 구독료 없이 자기 API 키나 로컬 모델로 쓰기를 원한다. 매일 여러 시간 켜 두는 주력 편집기로 쓰는 장면이 기준이다.

지금은 대상이 아님: 엔터프라이즈 망분리·SSO 조직, 실시간 팀 협업, VSCode 확장 생태계에 강하게 의존하는 사용자.

## Product Purpose

AI가 프로젝트 맥락을 제대로 이해하도록 만드는 데 집중한 IDE. 가볍고, 어떤 모델이든 붙여 쓸 수 있고, 에이전트·프롬프트·규칙을 전부 파일로 열어 고칠 수 있다.
성공: 사용자가 2주 동안 다른 IDE 없이 주력으로 쓰고, AI 답변이 맥락 덕분에 더 정확하면서 토큰은 덜 쓴다.

## Positioning

로컬 맥락 엔진이 질문마다 관련 코드를 구조 그래프·검색·변경 이력·프로젝트 메모리에서 골라 토큰 예산 안에 조립하고, 무엇을 왜 넣었는지 인스펙터로 전부 보여준다. 숨겨진 시스템 프롬프트가 없다. 모델에 종속되지 않는다.

VS Code·Cursor·Windsurf는 "파일과 채팅", Orca는 "병렬 에이전트"가 중심이다. Lantern은 **"코드와 에이전트가 하는 일을 지도로 보여주는 IDE"**다. 일의 단위는 파일이 아니라 작업이고, 가운데 화면은 편집기와 코드 지도를 오간다. 에이전트가 어디를 보고 고쳤는지(발자취), 이 수정이 어디까지 닿는지(영향 반경), 팀이 정한 규칙이 어느 코드에 걸려 있는지(기억 지도)를 같은 지도 위에서 본다. 자세한 설계: `docs/차별화_작업과_지도.md`.

## Operating Context

- 편집, 파일 탐색, 검색, 터미널, 언어 서버 진단을 한 창에서 쓴다. 배치와 단축키는 VS Code와 같아 옮겨 오는 비용이 없어야 한다.
- 왼쪽 첫 보기는 "작업" 목록이다. 작업마다 대화, AI에 보낸 맥락, 읽고 고친 파일, 되돌리기가 묶인다. 대화는 오른쪽에서 한다.
- 가운데는 편집기와 코드 지도를 전환한다(제목 표시줄 가운데, Ctrl+Alt+M). 프로젝트를 열면 지도부터 보인다.
- 에이전트의 파일 변경과 명령 실행은 Diff/명령을 보여주고 사용자 승인 후 적용한다. 파일 수정 승인 카드에는 영향 반경(호출자·테스트·함께 바뀌던 파일·위험도)이 붙는다.
- 설정은 `.lantern/config.toml`, 에이전트는 `.lantern/agents/*.md`, 프로젝트 메모리는 `.lantern/memory/*.md`. 파일로 고치는 방식이 1급이고, 화면 설정은 그 파일을 편하게 고치는 수단이다.
- 비용은 모델별 가격으로 계산하고 월 한도를 둔다.

## Capabilities and Constraints

- 맥락 엔진(lantern-context): Rust, tree-sitter, SQLite FTS5. CLI와 MCP 서버로도 제공.
- 모델 라우터: Anthropic Messages API, OpenAI 호환 API(OpenAI, Ollama, LM Studio, vLLM, OpenRouter).
- 에이전트 도구 10종, Diff 승인, 명령 허용 목록, 프로젝트 밖·.git 쓰기 거부.
- 언어 서버(LSP) 중계, PTY 터미널, 텍스트 검색, 명령 팔레트.
- 아이콘은 VS Code Codicons(CC BY 4.0). 외부 폰트·CDN을 쓰지 않는다(오프라인 동작, CSP).
- 제품명: Lantern IDE (2026-09-25, KHALA에서 변경. 상표 검색은 남음). 미정: 요금제.

## Brand Commitments

- 육각형 로고와 청록(#22d3ee)→남색(#6366f1) 계열을 Lantern 브랜드로 확정 (app/app-icon.png).
- 배치와 조작은 VS Code를 따른다. 그러나 로고만 바꾼 VS Code 복제품처럼 보이면 안 된다.
- 피해야 할 인상: 게이밍·네온(빛나는 테두리, 과한 그라데이션), 장난감 같은 가벼움, 낯선 조작.
- 말투: 한국어, 짧고 정확하게. 과장하지 않는다.

## Evidence on Hand

- 맥락 엔진 실측: jellySafe(TS 880개 파일)에서 초기 인덱싱 2.5초, 조립 p95 10ms, 토큰 53% 절감 (eval/결과.md).
- 사용자 후기, 고객, 가격, 비교 벤치마크는 아직 없다. 만들어 내지 않는다.

## Product Principles

1. 맥락이 먼저다: AI에게 올바른 코드를 보여주는 능력이 핵심 자산이다.
2. 숨기지 않는다: 보낸 파일, 프롬프트, 토큰, 비용을 언제든 볼 수 있다.
3. 사용자가 소유한다: 설정·에이전트·메모리는 로컬 텍스트 파일이다.
4. 가벼움은 측정한다: 수치와 측정 조건으로만 말한다.
5. 익숙함은 지킨다: 편집 조작과 단축키는 개발자가 이미 아는 방식을 따른다. 구조(작업·지도)는 달라도 손에 익은 동작은 그대로다.
6. 보여주고 맡긴다: 에이전트에게 일을 맡길수록, 무엇을 보고 무엇을 바꿨고 어디까지 닿는지를 한눈에 보여줘야 한다.

## Accessibility & Inclusion

키보드만으로 모든 기능을 쓸 수 있어야 한다(명령 팔레트, 단축키). 포커스 표시를 항상 보이게 한다. 텍스트 대비는 WCAG AA 이상.
